<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feed</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="posts mt-20">
        <h2 class="text-2xl font-bold text-gray-800 mb-4 text-center">Your Feed</h2>
        <a class="absolute left-1/2 transform -translate-x-1/2 top-4 px-4 py-3 bg-red-400 text-white mb-5 font-semibold rounded-lg shadow-lg" href="/profile">Your Profile</a>
        <div class="postcontainer mt-5">

            
                <% feed.forEach(item =>{ %>
                    <div class="post max-w-md mx-auto p-4 bg-gray-200 rounded-lg mt-5 border-2 border-black">
                    <h2 class="text-blue-400 mt-0 p-0 leading-none"><%= item.userID.username %></h2>
                <p class="text-sm tracking-tight"><%= item.content %></p>
                <a class="mt-4 inline-block"><%= item.likes.length %> likes</a>
                <div class="btns flex gap-4 mt-2">
                    <a class="text-red-500" href="/like/<%= item._id %>">
                       <%- item.likes.indexOf(user._id)==-1?'Like':'Unlike' %>
                    </a>
                    <% if (item.userID._id.equals(user._id)) { %>
                        <a class="text-green-500" href="/delete/<%= item._id %>">Delete</a>
                      <% } %>
                    <% if (item.userID._id.equals(user._id)) { %>
                        <a class="text-green-500" href="/edit/<%= item._id %>">Edit</a>
                      <% } %>
                </div>
                </div>
                <%}) %>
        </div>
    </div>
    
</body>
</html>